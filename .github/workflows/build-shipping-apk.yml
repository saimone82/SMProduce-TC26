name: Build Shipping APK
on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'shippingapp/**'
      - 'settings.gradle'
      - 'build.gradle'
      - '.github/workflows/build-shipping-apk.yml'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - name: Install SDK
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Build Shipping APK
        run: gradle :shippingapp:assembleDebug
      - uses: actions/upload-artifact@v4
        with:
          name: SMProduce-Shipping-APK
          path: shippingapp/build/outputs/apk/debug/shippingapp-debug.apk
